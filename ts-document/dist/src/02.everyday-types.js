"use strict";
let obj = { x: 0 };
function printCode(pt) {
    console.log("the coordinate's x value is" + pt.x);
    console.log("the coordinate's y value is" + pt.y);
}
printCode({ x: 3, y: 7 });
function printCode2(pt) {
    console.log("the coordinate's x value is" + pt.x);
    console.log("the coordinate's y value is" + pt.y);
}
printCode2({ x: 3, y: 7 });
